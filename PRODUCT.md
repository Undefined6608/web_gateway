# Product

## Register

product

## Users

运维和开发人员在公司内部网络中使用系统集成网关，快速寻找系统入口、线上或测试地址、负责人、页面/后端/数据开发人员，以及登录后维护系统账号信息。

## Product Purpose

把分散在不同系统和文档里的集成信息集中到一个可检索的目录中。用户首先确认系统和地址，再根据权限进入管理端查看或维护账号与人员配置。

## Brand Personality

清楚、可靠、克制。界面应像一个熟悉的内部工具，减少装饰和等待，让信息优先于视觉效果。

## Anti-references

不要使用大面积空白的展示型首页、居中的营销页构图、低对比灰色文字、无意义的渐变、玻璃卡片、装饰性数据指标或看起来像 AI 生成的泛化 SaaS 模板。

## Design Principles

1. 先找到系统，再查看状态和地址。
2. 用明确的状态、标签和更新时间降低判断成本。
3. 公开目录的普通请求不接触密码；只有用户主动发起并通过一次性管理员认证后，才短暂显示该系统账号。
4. 密度服务于查找，不用卡片和动效制造噪声。
5. 动效只表达加载、刷新、成功、失败和页面切换等状态。

## Accessibility & Inclusion

目标为 WCAG 2.1 AA。状态不能只依赖颜色，所有关键操作支持键盘焦点和可读标签；尊重 `prefers-reduced-motion`，移动端优先保证内容不横向溢出。
